// const capitalizeFirstLetter = string => {
//   console.log('capitalizeFirstLetter');
//   return string.charAt(0).toUpperCase() + string.slice(1);
// };

// const setContactId = contacts => {
//   console.log(contacts);
//   const contactIds = [];
//   contacts.map(contact => contactIds.push(contact.id.replace(/\D/g, '')));
//   console.log('setContactId');
//   return 'id-' + (Math.max(...contactIds) + 1);
// };

// export default { capitalizeFirstLetter, setContactId };
